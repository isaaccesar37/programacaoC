#include <stdio.h>

    int main(){

        int a = -10;

        if (!(a > 0)) {//(!(a > 0)
            printf("A variavel e negativa\n");
            }   else {

            printf("A variavel e positiva.\n");
        }


    }